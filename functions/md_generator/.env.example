# local | gcp
APP_ENV=local

# GCP (Geminiはローカルでも実GCPに接続)
GCP_PROJECT_ID=deep-book-ocr
GCP_LOCATION=us-central1

# Output bucket
OUTPUT_BUCKET=deep-book-ocr-output

# local_runner.py で擬似CloudEventに使う入力JSON（実GCS）
LOCAL_INPUT_BUCKET=deep-book-ocr-temp-2538d0
LOCAL_INPUT_OBJECT=processed/sample_pdf/0.json

# Gemini
MODEL_NAME=gemini-1.5-flash
CHUNK_SIZE=10
